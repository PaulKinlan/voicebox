# Open questions

1. **The name.** `voicebox` is a working name, chosen so the repo exists today. Cheap to
   change; better to change before anything depends on it.
2. **Relationship to the existing projects.** The reading above is that this is a *new*
   platform that borrows parts — the voice UI from isocan, the tool/skill/Wasm model from
   CAP, the minimalism from pi — rather than being a component of either. If it should
   instead *reuse* CAP's runtime, that changes the architecture before a line is written.
3. **"This might be like the answer to this."** The remark about local project files
   arrived mid-sentence; the reading taken is that *a project on disk is the unit of work*.
   Confirm or correct.
