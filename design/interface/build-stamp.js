// serve.mjs replaces this response with the actual Git branch, commit and dirty state.
// A copied static page must not inherit a stamp it has not established.
document.getElementById('build-stamp').textContent = 'Unstamped static copy';
